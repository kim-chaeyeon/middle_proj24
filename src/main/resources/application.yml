spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/middle_proj
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  main:
    allow-bean-definition-overriding: true
  config:
    import: optional:application-secret.yml

custom:
  fileDirPath: C:/work/thumbnail
